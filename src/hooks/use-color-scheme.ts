/**
 * O visual do app foi desenhado para o tema claro (navy + âmbar).
 * O modo escuro do sistema invertia a paleta e deixava a UI estranha no celular.
 */
export function useColorScheme(): "light" {
  return "light";
}
